# ログイン画面 詳細設計 v1

クライアント要件：**原神のゲーム起動画面（ランチャー）のような体験**を、妖・和風神話の世界観に翻訳する。

---

## 0. 確定方針（前提）

1. **アート**：具象イラストは使わない。プロシージャル中心 + 抽象テクスチャのみ
2. **3D**：Three.js を MVP から導入（v2 待ちにしない）
3. **音**：BGM + 環境音あり（自動再生はしない、ユーザーアクションで起動）
4. **フォールバック**：本設計（option 2）が安っぽく仕上がった場合、純プロシージャル（option 1、Active Theory 系 code art）に切り替える退避路を用意する

---

## 1. ターゲット体験

> ページに到達すると、夜の坂の頂で立ち止まったような感覚になる。眼下に黄泉平坂、霧の底に朱の鳥居が消えていく。狐火が漂い、月が滲む。風と笛の音が遠くに聞こえる。中央に小さな短冊が浮かぶ。「参拝」と書かれている。

原神ランチャーの **「世界が先、UI は後」** の構図をそのまま借りる。サーバー名と御札は脇役にする。

---

## 2. シーン構成（Three.js シーンの全体像）

カメラ視点：**坂の頂から黄泉平坂を見下ろす**位置。視線はわずかに下向き。

```
                       ・・・     ← 月光（god rays）
                ╱ ╲
              ╱  月  ╲           ← 月（背後やや高所）
            ╱─────────╲
        山稜のシルエット          ← 奥の遠景
       ╱╲   ╱╲   ╱╲             
       │   │   │   │            ← 鳥居列（朱、坂を下って小さくなる）
       ┴───┴───┴───┴
     ░░░░░░░░░░░░░░░░░░          ← 霧（坂の底に溜まる）
   ❀     ❀       ❀  ❀            ← 彼岸花（近景）
        ✦       ✦                ← 狐火（粒子、全域）
                                  
            ┌───┐
            │参 │                ← 御札（DOM、Three.jsの上に重ねる）
            │拝 │
            └───┘
```

### 主要要素一覧

| 要素 | 実装手法 | レイヤー |
|------|---------|---------|
| 空 | グラデーション + 微小ノイズ shader | 最奥 |
| 月 | sphere + emissive corona shader | 奥 |
| 月光（god rays） | post-process（後段） | 奥〜中 |
| 山稜 | 平面 + heightmap displacement shader（黒シルエット） | 奥 |
| 鳥居列 | ローポリ自作 + InstancedMesh（20体、サイズ徐減） | 中〜近 |
| 霧 | exponential fog + 流動 shader プレーン | 全域 |
| 彼岸花 | stylized billboard or 単純メッシュ + emissive | 近景 |
| 狐火 | GPU particles（`THREE.Points` + 自作 shader） | 全域 |
| 花弁・紙片 | `THREE.Points`、ゆっくり下降 | 近景 |
| 御札 | DOM 要素、canvas の上にオーバーレイ | UI |
| サーバー名 | DOM、御札の上に配置 | UI |

---

## 3. カメラ仕様

| 項目 | 値 |
|------|---|
| 投影 | Perspective |
| FOV | 35°（やや圧縮、シネマ寄り） |
| 高さ（Y） | 1.5（人の目線） |
| 注視点 | (0, 0.8, -10) |
| マウス追従 | yaw ±1.5°、pitch ±0.8°（リニア補間、0.5s ease-out） |
| 呼吸 | yaw ±0.4° を 8s 周期サイン波で常時 |
| クリック後 | 4s かけて Z=-8 まで前進、霧が満ちる |

---

## 4. ライティング

3D シーンには真の光源は1つだけ置く（月）。残りは**emissive と post-process**で表現。

| 光源 | 種類 | 役割 |
|------|------|------|
| `DirectionalLight` | 月光（弱い、青白） | 山稜・鳥居の反対側を冷たく沈める |
| `AmbientLight` | 弱い ink-900 | 完全な黒つぶれ防止 |
| Emissive（鳥居・月・狐火・彼岸花） | — | 光源風の見た目を出す主役 |

post-process：
- **Bloom**（threshold=0.6, intensity=0.8） — 狐火・月・emissive を滲ませる
- **Chromatic aberration**（弱め） — 画面端だけ
- **Vignette**（黒、強め） — 画面の縁を闇に沈める
- **Film grain**（極弱） — 安っぽくならない範囲で和紙の繊維感

---

## 5. シェーダー方針

すべて GLSL で自作。重い raymarch は避け、2D ノイズ + ジオメトリの組み合わせで雰囲気を出す。

### 5.1 霧 shader
- 入力：fbm（fractal Brownian motion、3〜5 octaves）+ time scroll
- 色：`mist-100` を α 0.0〜0.6 でブレンド
- 配置：水平プレーン3枚を高さ違いで重ねる
- パフォーマンス：fbm は costly、必要なら precomputed noise texture に置き換え

### 5.2 月 shader
- emissive sphere（mist-glow base）
- corona：放射状ノイズで縁を不均一に
- 太陽コロナ的なちらつきを 4s 周期で

### 5.3 鳥居 emissive
- vertex shader で y 値による emissive intensity（上にいくほど薄く光る）
- base color: vermilion-500
- shadow color: vermilion-700

### 5.4 狐火 particle
- `THREE.Points` + custom shader
- 各粒子に乱数 seed、寿命、size、色相を持たせる
- 色相は mist-glow 〜 gold-300 の間でランダム
- 上昇しながら左右に揺れて消える
- 同時存在数：50 体（モバイルは 25 体）

### 5.5 山稜
- 横長プレーンに heightmap noise を vertex displacement
- color: ink-900（ほぼ真っ黒）

### 5.6 彼岸花
- 近景の数株のみ
- ステム + 花の simple geometry、emissive crimson-500
- vertex shader で微風の sway（amplitude 0.05 rad）

---

## 6. パーティクル・粒子

| 種類 | 数 | 動き | 色 | 寿命 |
|------|---|-----|---|-----|
| 狐火 | 50（mobile 25） | 上昇 + 左右揺らぎ | mist-glow 〜 gold-300 | 4〜8s |
| 紙片・花弁 | 30 | ゆっくり下降 + 回転 | crimson-500 / washi-100 | 12〜20s |
| 塵 | 100 | ほぼ静止、ブラウン運動 | mist-300 α0.3 | 永続 |

---

## 7. アニメーション・タイミング

### 7.1 ページロード（0〜4s）

```
0.0s  画面真っ黒、ロード中
0.5s  カメラ位置確定、霧が薄く流れ始める
1.0s  山稜・鳥居がフェードイン（ink から滲み出る）
1.8s  月が滲み出る + corona 開始
2.2s  狐火が湧き始める
2.8s  サーバー名がじわっと浮かぶ（gold-100、blur 10px → 0px）
3.5s  御札が下から漂着、揺れ始める
4.0s  操作可能
```

### 7.2 idle（操作可能後、ループ）

- カメラ呼吸：yaw ±0.4°、8s 周期、永続
- 霧：noise scroll、永続
- 狐火：常時湧き出る（前項の数を維持）
- 御札：±1.5° の揺れ、3s 周期
- 月の corona：脈動、8s 周期

### 7.3 御札ホバー

- 御札の周囲に青白い炎が点る（duration 200ms）
- 御札の揺れ振幅 +50%、周期 +50% 加速
- 最寄りの狐火 1〜2 体が御札に向かって寄ってくる
- ambient 音の音量を +3dB
- 別ホバー要素にカーソルが移ったら 400ms で元に戻る

### 7.4 御札クリック → 認証へ

```
0.0s   御札クリック、鈴の SFX
0.2s   御札を中心に紙吹雪が放出（200 体）
0.4s   カメラが Z 方向に前進開始（dur 4s, ease-out）
1.0s   霧が画面下部からせり上がる
2.5s   霧が画面の 70% を覆う
3.5s   ホワイトフラッシュ（mist-100、α 0→1→0）
4.0s   Discord OAuth へ window.location 遷移
```

### 7.5 認証完了 → ハブ遷移（v2 で精緻化）

MVP では Discord からの戻りは **黒からのフェードイン + 短いカメラ前進**で繋ぐ。完全なワープ演出は v2。

---

## 8. UI 要素配置

| 要素 | 位置 | サイズ | 注記 |
|------|-----|-------|------|
| サーバー名 | 中央上 30% | display-xl（64px） | gold-100、Yuji Syuku、文字 blur フェードイン |
| 御札 | 中央下 30% | 80×240px | 縦書き「参拝」、gold-500 縁 |
| スキップ「結界を抜ける」 | 右上 | body-sm | 常時表示（reduced-motion でも） |
| 音量トグル | 右下 | アイコンのみ | 既定はミュート |
| フッタ表記（バージョン等） | 左下 | caption | 透明度 0.4 |

---

## 9. 音設計

### 9.1 構成レイヤー

| 層 | 内容 | 音量 | 役割 |
|----|------|-----|------|
| BGM | 和風器楽（尺八・篳篥・箏・ドローン） | -18dB | 主旋律 |
| Ambient ground | 風、遠雷のドローン | -24dB | 静寂感 |
| Ambient detail | 葉擦れ、虫の声、遠い太鼓 | -28dB | 細部 |
| SFX hover | 御札の紙擦れ | -15dB | 即応 |
| SFX click | 鈴 + 紙の捌く音 | -10dB | 決定感 |

### 9.2 自動再生方針

ブラウザの autoplay policy のため、**初回はミュート起動**。御札の参拝アクションで「初回のみ音量フェードイン」させる。
- 既定：mute
- 右下の音量トグルで切り替え可
- 一度有効化したらユーザー設定として localStorage に保存
- 次回以降はその設定で起動（ただしブラウザ規制で完全自動再生できない場合は再度ミュート起動 → 数秒以内のクリックで復元）

### 9.3 アクセシビリティと配慮

- `prefers-reduced-motion: reduce` の場合、**音もデフォルトオフ**
- 大きな音や急激な音量変化を避ける
- ユーザーがミュートしたら以降はミュート維持

### 9.4 実装

- ライブラリ：**Howler.js**（cross-browser、Web Audio fallback、軽量）
- ファイル形式：mp3（互換性優先）+ ogg（Firefox 等で軽量）
- BGM はループポイントを設計し crossfade ループ
- レイヤーは Howler の Group で並列管理

### 9.5 素材調達（フリー素材想定）

| 種類 | 候補 | ライセンス確認 |
|------|------|---------------|
| BGM | DOVA-SYNDROME, 魔王魂, OtoLogic | クレジット要件確認 |
| 環境音 | freesound.org | CC0 のものに限定 |
| SFX | zapsplat, freesound.org | 同上 |

すべての素材のライセンスとリンクをリポジトリに `assets/audio/CREDITS.md` として記録する。

---

## 10. パフォーマンス予算

| 項目 | desktop | mobile |
|------|---------|--------|
| 目標 FPS | 60 | 30 |
| triangle count | <50k | <20k |
| draw calls | <60 | <30 |
| texture（合計） | <5MB | <2MB |
| particle 数 | <500 | <250 |
| GLSL fragment ops | 中程度 | 低（fbm octaves 削減） |

### 軽量化策

- InstancedMesh で鳥居列を 1 draw call
- precomputed noise texture（fbm を実行時計算しない選択肢）
- bloom を post-process pass で 1 回のみ
- DPR を `min(devicePixelRatio, 1.5)` にクランプ
- 低スペック検出時は particle・octave を半減

### 計測

- Cloudflare Web Analytics で LCP / CLS
- Sentry の performance で初期化時間
- localStorage に「初回 ms」記録、平均値が 4s 超えたら警告

---

## 11. アクセシビリティ・救済

### 11.1 `prefers-reduced-motion: reduce` 時

- カメラ呼吸停止
- 霧の流動停止（静止画として表示）
- 狐火・粒子停止、密度を 1/3 に削減
- 御札の揺れ停止
- bloom を弱める（intensity 0.3 まで）
- 音はデフォルトオフ

### 11.2 スキップ機構

右上の「**結界を抜ける**」リンクは常時可視。クリックで全演出をスキップして OAuth へ直行。

### 11.3 キーボード

- Tab：御札 → スキップ → 音量トグル の順にフォーカス可能
- Enter / Space で御札起動
- フォーカスリングは gold-500、outline 2px offset 2px

### 11.4 端末変更時の救済

- localStorage に「2回目以降は短縮版を見せる」フラグ
- 初訪問は 4s 演出、2回目以降は 1.5s 演出
- localStorage が無い／別端末では 4s 演出だが、スキップ表示で逃げ道を残す

---

## 12. ブレイクポイント別の挙動

| 端末 | 解像度 | 採用するシーン |
|------|--------|---------------|
| desktop（>=1280px） | 高密度 | フル版、particle 500、bloom フル |
| tablet（768〜1280px） | 中密度 | particle 300、bloom 中 |
| mobile（<768px） | 低密度 | particle 250、bloom 弱、霧プレーン2枚 |
| 低スペック検出時 | — | particle 100、霧プレーン1枚、bloom off |
| reduced-motion | — | §11.1 の通り |

検出ロジック：
```ts
const lowSpec =
  navigator.hardwareConcurrency < 4 ||
  (navigator as any).deviceMemory < 4;
```

---

## 13. フォールバック路（option 2 → option 1）

「抽象テクスチャ込みで作ってみたが安っぽい」と判定された場合の退避路：

### 何を捨てるか
- すべてのテクスチャ画像（ノイズ・紙繊維・墨滲み）
- 山稜の displacement texture（純シルエット平面に）
- 月の corona texture（純シェーダー corona に）

### 何で代替するか
- 純粋な GLSL noise（fbm のみ、テクスチャ不要）
- ジオメトリック表現を強める（線描・グリッド・幾何学パターン）
- 色数を絞る（ink-950, mist-glow, gold-500 のみ）

### 結果像
Active Theory / Bruno Simon / Awwwards 受賞作系の **「コードアート」** に近づく。
具象的な「鳥居の絵・彼岸花の絵」がなくなり、抽象的な「線と光の構成」になる。
原神ランチャーとは別物の方向だが、**それ単体で完結した美学**を持つ。

### 切り替え判断のタイミング
- M4 序盤で「最初の見栄え」が出た時点
- 関係者で 1〜2 日見比べて判定
- 切り替えコスト：シェーダー差し替えが主、ジオメトリは流用可、所要 2〜3 日

---

## 14. 必要素材リスト

| カテゴリ | 内容 | 入手 | 備考 |
|---------|-----|------|------|
| 3D model | 鳥居（low-poly、~500 tri） | 自作 or Sketchfab CC0 | 1 体だけ用意し instancing |
| Texture | ノイズ（fbm precomputed） | JS で自動生成 | 1024×1024、PNG |
| Texture | 紙繊維（option 2 のみ） | freepik 等 or 自作 | 512×512、PNG |
| Audio | BGM（和風 1曲、loop 90s） | DOVA-SYNDROME 等 | mp3 + ogg |
| Audio | Ambient（風・葉擦れ・遠雷） | freesound.org | mp3 + ogg、CC0 |
| Audio | SFX（鈴・紙擦れ） | freesound.org | mp3 + ogg、CC0 |
| Font | Yuji Syuku, Shippori Mincho B1, Noto Sans JP | Google Fonts | サブセット化 |

---

## 15. 実装スケルトン

### 15.1 ファイル構成

```
apps/web/src/components/login/
├ Stage.astro             ルートコンポーネント
├ Stage.client.tsx        Three.js のマウント先
├ scene/
│  ├ index.ts             シーン全体のセットアップ
│  ├ camera.ts            カメラ + breathing
│  ├ moon.ts
│  ├ torii.ts
│  ├ fog.ts
│  ├ higanbana.ts
│  ├ particles/foxfire.ts
│  ├ particles/petals.ts
│  └ post.ts              post-process
├ shaders/
│  ├ fog.frag.glsl
│  ├ moon.frag.glsl
│  ├ torii.vert.glsl
│  └ noise.glsl           共通 fbm
├ audio/
│  ├ index.ts             Howler セットアップ
│  └ tracks.ts
└ ui/
   ├ Ofuda.astro          御札
   ├ ServerName.astro
   ├ SkipLink.astro
   └ MuteToggle.astro
```

### 15.2 マウント方針

- Astro ページは SSG/SSR で UI（御札・サーバー名・スキップ）を出す
- Three.js シーンは client island として lazy load（`client:idle` または `client:visible`）
- 初期 LCP には Three.js を含めない（背景は CSS グラデのまま、後から canvas が乗る）

### 15.3 主要パッケージ

```
three                       3D 本体
postprocessing             bloom 等の post pass
howler                     音
gsap                       UI アニメ（既定）
```

---

## 16. 引き継ぎ資料 / デザインシステムへの影響

本設計の確定により、以下の更新が必要：

### 引き継ぎ資料 v3 の更新箇所
- §3 技術スタック：Three.js を「v2」→「**MVP から導入**」に変更、`Howler.js` 行を追加
- §1.1 開発方針：「BGM/環境音あり」「具象イラスト不採用」を追記
- §8 ログイン画面 設計仕様：レイヤー構成を本資料の §2 に置き換え
- §9 ログイン画面 素材調達計画：本資料の §14 に置き換え
- §14 アクセシビリティ：音のデフォルトミュート規約を追記
- §20 マイルストーン：M4 に Three.js シーン構築を統合、M9 を削除

### デザインシステム v1 の更新箇所
- §10 画像・アセット：「具象イラスト不採用、プロシージャル + 抽象テクスチャ優先」を追記
- 新節 §10.5「音」：本資料 §9 のサマリ + ミュート規約

これらは別資料の更新として後追いで反映する。

---

## 17. 残課題（M4 着手前に潰す）

1. 鳥居 3D モデルを自作するか CC0 を拾うか決定
2. BGM の候補曲を 3〜5 曲ピックアップ、関係者で投票
3. ノイズテクスチャ生成スクリプトを書く（1〜2 日）
4. 「フル版／短縮版」の判定ロジック仕様（localStorage キー名・期限）
5. mobile 検出の閾値の最終確認
6. 「結界を抜ける」スキップの遷移先（直接 OAuth or 演出スキップ後の御札表示）
7. 認証完了後の戻り遷移の繋ぎ（v2 で精緻化、MVP は黒フェード）

---

## 18. 一行サマリー

**「世界が先、UI は後」を Three.js + プロシージャル + 和風器楽で組み上げ、安っぽく仕上がったらコードアートに振り切る退避路を確保する。**
