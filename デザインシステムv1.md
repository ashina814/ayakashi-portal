# 妖・和風神話Webサイト デザインシステム v1

全ページ共通の基盤。各画面の詳細仕様（ログイン・ハブ・マイページ等）はこの上に乗る前提で別資料化する。

> 大原則：**「夜の社（やしろ）に灯る一筋の行燈」**。
> 闇を主役に、光（金・青白）と朱で要点を照らす。装飾は空白に語らせ、冗長な縁取りや影で塗り潰さない。

---

## 1. デザイン原則

| 原則 | 意図 | 反例 |
|------|------|------|
| 闇が地、光が点 | 背景は深い闇、フォーカスのある要素だけ金や青白で照らす | 全面に金縁・全面にネオン |
| 静謐 > 装飾 | 余白で「気配」を残す、要素を詰め込まない | 帯を全部埋めるレイアウト |
| 呼応する | カーソル・スクロール・ホバーで微細に光が動く | 静止した画面 |
| 紙と札の質感 | フラットUIに「和紙の繊維」「墨の滲み」など微細なテクスチャを乗せる | 純粋なベタ塗り |
| 世界観は要所だけ | 全UIを「参拝」「お祓い」で埋め尽くさない | 全ボタンが古語 |

---

## 2. ボイス & ワーディング

UI ラベルは**普通の言葉が基本**、要所だけ世界観の言葉を当てる。過剰だとUXが落ちるので頻度を縛る。

### 採用ワード対照表

| 機能 | 通常UI | 採用ワード | 採用箇所 |
|------|-------|----------|---------|
| ログイン | Sign in | **参拝** | ログイン画面の主CTAのみ |
| ログアウト | Sign out | **退散** | マイページ右上のメニュー |
| 強制再取得 | Refresh | **お祓い** | ロール再取得ボタンのみ |
| プロフィール編集 | Edit profile | 「己を記す」 | マイページ編集モード見出し |
| 保存 | Save | 保存（変えない） | 機能を阻害するため |
| エラー | Error | 「霧が立ち込めて見通せません」 | エラーページ本文 |
| 404 | Not found | 「道に迷われたようです」 | 404ページ |
| 権限なし | Forbidden | 「結界が阻んでいます」 | 403ページ |
| ロード中 | Loading | 「気配を辿っています…」 | 大きめのスピナー時のみ |

### 適用ルール

- ボタン文言は**1画面に1〜2個まで**世界観ワードを許容、それ以上は普通語
- 機能性が下がる用途（保存・キャンセル・削除）には世界観ワードを使わない
- 重要な世界観ワードには `title` 属性で標準語を補足（新規メンバーに親切）

---

## 3. カラー

### 3.1 トーンの原則
- ダークが基本。ただし**Wiki本文エリアだけ「和紙トーン」**を許容（夜の社で行燈を灯して読む構図）
- 純白 / 純黒 は使わない。常にわずかに紫・青・茶を含める

### 3.2 パレット

```
ink (墨)        — 背景・地
  ink-950   #0a0509   ベース最深
  ink-900   #14090f
  ink-800   #1f1219   面
  ink-700   #2a1c25   上面・カード
  ink-600   #3a2733   ボーダー強

mist (幽)       — 青白い光、cool accent
  mist-100  #e7eef3   テキスト最明
  mist-300  #b8cad8
  mist-500  #6f8fa6
  mist-700  #3d5566
  mist-glow #b8e0ff   炎・グロー専用

vermilion (朱) — 鳥居・警告寄り
  vermilion-300 #d99c8c
  vermilion-500 #b04438   鳥居の朱
  vermilion-700 #621c14

crimson (紅)   — 彼岸花・強調
  crimson-300 #de7f8a
  crimson-500 #c1283a     アクセント本命
  crimson-700 #8a0d1c

gold (金)      — テキスト・縁・primary
  gold-100  #f5e7c4
  gold-300  #e0c684
  gold-500  #b89540        くすみ金、本命
  gold-700  #8a6d24

washi (和紙)   — Wiki本文サーフェス
  washi-50  #f5efe1
  washi-100 #ebe2c9
  washi-200 #d8c9a3        罫線・区切り
  washi-700 #5c5340        washi上の本文テキスト
  washi-900 #2e2818        washi上の見出し

moss (苔)      — success
  moss-500  #6b8a4e
```

### 3.3 セマンティック割当て

| 役割 | トークン |
|------|--------|
| `bg-base` | ink-950 |
| `bg-surface` | ink-800 |
| `bg-elevated` | ink-700 |
| `bg-washi` | washi-50（Wiki本文のみ） |
| `text-primary` | gold-100 |
| `text-secondary` | mist-300 |
| `text-muted` | mist-500 |
| `text-on-washi` | washi-700 |
| `border-base` | ink-600 |
| `border-strong` | gold-700 |
| `accent` | crimson-500 |
| `link` | gold-300（hover で gold-100） |
| `success` | moss-500 |
| `warning` | gold-500 |
| `danger` | crimson-500 |
| `info` | mist-500 |
| `glow` | mist-glow |

### 3.4 コントラスト

WCAG 2.1 AA を最低ライン：

- `gold-100` on `ink-950`：13.4:1 ✓
- `mist-300` on `ink-950`：8.9:1 ✓
- `mist-500` on `ink-950`：4.6:1 ✓ ギリギリ、本文では使わない（補助テキストのみ）
- `gold-500` on `ink-950`：5.4:1 ✓
- `crimson-500` on `ink-950`：3.4:1 ✗ → 大きい文字・装飾用途のみ。本文には使わない
- `washi-700` on `washi-50`：8.2:1 ✓

---

## 4. タイポグラフィ

### 4.1 書体

| 役割 | 書体 | 提供 |
|------|------|------|
| Display（サーバー名・大見出し） | **Yuji Syuku** | Google Fonts |
| Body 和（本文・読み物） | **Shippori Mincho B1** | Google Fonts |
| UI / データ表示 | **Noto Sans JP** | Google Fonts |
| Mono（管理画面の数値・コード） | **JetBrains Mono** | Google Fonts |

すべて `font-display: swap` + サブセット化（日本語は `unicode-range` で必要グリフのみ）。

### 4.2 タイプスケール（modular 1.250）

| トークン | サイズ | 用途 | line-height |
|---------|------|-----|-------------|
| `display-xl` | 64px | ログインのサーバー名 | 1.1 |
| `display` | 48px | ページタイトル | 1.15 |
| `h1` | 36px | セクション最上位 | 1.2 |
| `h2` | 28px | サブセクション | 1.3 |
| `h3` | 22px | カード見出し | 1.35 |
| `h4` | 18px | 小見出し | 1.4 |
| `body-lg` | 18px | Wiki 本文 | 1.8 |
| `body` | 16px | UI 本文 | 1.7 |
| `body-sm` | 14px | 補助テキスト | 1.6 |
| `caption` | 12px | キャプション・タグ | 1.5 |

### 4.3 規約

- 和文 line-height は **1.7 以上** が原則（漢字の高さで詰まりやすいため）
- 英数字混在の見出しは `font-feature-settings: "palt"` で詰める
- 縦書き要素（御札の「参拝」など）は `writing-mode: vertical-rl` + `text-orientation: upright`
- letter-spacing は和文には**ほぼ付けない**（日本語フォント設計を尊重）。ただし display のみ `0.05em` まで許容
- `font-weight` は 400 / 500 / 700 の3段。Yuji Syuku は単一ウェイトのみ

---

## 5. スペーシング・レイアウト

### 5.1 スペーシングスケール（4pt base）

```
0   2   4   8   12   16   24   32   48   64   96   128   192
```

トークン名: `space-0` 〜 `space-192`。

### 5.2 コンテナ幅

| 用途 | 最大幅 |
|------|------|
| 没入演出（ログイン・ハブ） | 全画面（`100vw / 100dvh`） |
| アプリ画面（マイページ・運営） | `max-w-screen-lg`（1024px） |
| Wiki 本文 | `max-w-prose`（約 65ch） |

### 5.3 余白原則

- セクション間：`space-64` 〜 `space-96`
- カード内：`space-24`〜`space-32`
- フォームフィールド間：`space-16`
- 文字と縁の最小距離：`space-12`
- **空白は装飾**。詰めない、埋めない

---

## 6. シェイプ（角・形状）

### 6.1 角丸スケール

| トークン | 値 | 用途 |
|---------|---|-----|
| `radius-none` | 0 | 既定（襖・面・カード） |
| `radius-sm` | 2px | ボタン・入力 |
| `radius-md` | 4px | 控えめなカード |
| `radius-lg` | 8px | 例外（モーダル等で柔らかさが欲しいとき） |
| `radius-full` | 9999px | アバター・印章型バッジ |

**基本は `radius-none` または `radius-sm`**。丸みを多用すると和風の硬質感が消える。

### 6.2 形状語彙

世界観コンポーネントの典型形：

- **御札（短冊）**：縦長矩形、上端に小さな三角の切り込みまたは梵字飾り、`radius-sm` 全角
- **印章**：正方形 or 六角、`radius-none`、内側に紋章 SVG
- **巻物カード**：上下にゴールドの 1px ライン、左右はフェードアウト
- **障子モーダル**：半透明白（`rgba(245, 239, 225, 0.92)`）+ 格子模様の薄いオーバーレイ
- **襖**：大型のフラット矩形、ホバーで右にスライド開きする

---

## 7. エレベーション（影・光）

ダークUIなので「黒い影」より「光のグロー」で elevation を表現する。

| レベル | 効果 | 用途 |
|-------|-----|------|
| `elev-0` | なし | 地・背景 |
| `elev-1` | `inset 0 0 0 1px rgba(184, 149, 64, 0.18)` | カードの控えめな縁 |
| `elev-2` | `0 0 24px 0 rgba(184, 224, 255, 0.10), inset 0 0 0 1px rgba(184, 149, 64, 0.30)` | フォーカス・ホバー時 |
| `elev-3` | `0 0 48px 0 rgba(184, 224, 255, 0.18), 0 8px 32px 0 rgba(10, 5, 9, 0.7)` | モーダル・最重要要素 |
| `elev-flame` | `0 0 16px 0 rgba(184, 224, 255, 0.40)` をパルス | 御札ホバー時の青白い炎 |

ルール：
- 黒影だけの elevation は使わない（必ず glow と組み合わせる）
- グロー色は **mist-glow / gold-300 / crimson-300** のいずれか
- パルスは `prefers-reduced-motion` で停止

---

## 8. モーション

### 8.1 Duration トークン

| トークン | 値 | 用途 |
|---------|---|-----|
| `dur-instant` | 100ms | 状態変化の即時反映（チェックボックス等） |
| `dur-fast` | 200ms | ホバー・フォーカス |
| `dur-base` | 400ms | カード展開・モーダル |
| `dur-slow` | 800ms | ページ間遷移 |
| `dur-atmo` | 1500ms | 雰囲気アニメ（霧・パララックス） |
| `dur-cinema` | 2500ms+ | ログイン演出など限定 |

### 8.2 Easing トークン

| トークン | 値 | 用途 |
|---------|---|-----|
| `ease-out` | `cubic-bezier(0.22, 1, 0.36, 1)` | 主役。ほぼこれ |
| `ease-in-out` | `cubic-bezier(0.65, 0, 0.35, 1)` | ループ系 |
| `ease-spring` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 軽いバウンス（御札の揺れ等） |
| `ease-linear` | `linear` | グロー脈動など定常 |

### 8.3 原則

- 急停止しない（`ease-out` を基本）
- atmospheric な要素は常時微動（霧・光・御札の揺れ）
- ユーザー操作の反応は `dur-fast` 以下、装飾は `dur-base` 以上
- **`prefers-reduced-motion: reduce` 時の挙動を必ず実装**：
  - duration を `dur-instant` に切り詰め
  - 雰囲気アニメ（パララックス・脈動・揺れ）は完全停止
  - フェードのみ残す

---

## 9. アイコン

- **ベース**：Phosphor Icons（`thin` weight, 1px stroke 相当）
  - 線が細いと和風の繊細さと相性が良い
- **カスタム**：印・札・紋・鳥居・狐火・彼岸花などをインライン SVG で自作
- 混在ルール：機能アイコンは Phosphor、文化記号はカスタム
- サイズ：16 / 20 / 24 / 32 / 48px の 5 段
- 色：原則 `currentColor`、装飾用途のみグラデーション可

---

## 10. 画像・アセット

- **具象イラスト（鳥居の絵・彼岸花の絵）は不採用**。Three.js + GLSL シェーダーによるプロシージャル表現を優先
- 抽象テクスチャ（紙繊維・墨の滲み・ノイズ）のみ使用可。`assets/textures/` に配置
- 抽象テクスチャは生成スクリプトでビルド可能なものを優先（fbm noise 等）
- 写真は使わない
- ラスタ画像（テクスチャ）は WebP + AVIF を必ずペアで出力
- アイコンや簡易図形は SVG（ファイルサイズ・拡縮・色変更で有利）
- 静的アセットは Astro のビルド時最適化に乗せる

## 10.5 音

ログイン画面で **BGM + 環境音 + SFX** を採用。他ページでは原則無音。

### レイヤー構成と音量

| 層 | 用途 | 既定音量 |
|----|-----|---------|
| BGM | 和風器楽（尺八・篳篥・箏・ドローン） | -18dB |
| Ambient ground | 風・遠雷ドローン | -24dB |
| Ambient detail | 葉擦れ・虫・遠太鼓 | -28dB |
| SFX hover | 御札の紙擦れ等 | -15dB |
| SFX click | 鈴・紙の捌き等 | -10dB |

### 規約

- **既定はミュート**（ブラウザ autoplay policy 準拠）
- ユーザー操作で起動、設定は `localStorage` に保持
- `prefers-reduced-motion: reduce` 時は音もデフォルトオフ
- 急激な音量変化を避ける、フェードで遷移
- 大きな音（>-6dB）は使わない
- すべての音素材は `assets/audio/CREDITS.md` にライセンスを記録

### 実装

- ライブラリ：**Howler.js**
- ファイル形式：mp3 + ogg のペア
- BGM はシームレスループ点を設計、crossfade で繋ぐ
- レイヤーは Howler の Group で並列管理

---

## 11. コンポーネント基本パターン

| コンポーネント | スタイル | 状態 |
|--------------|--------|------|
| Button (primary) | 御札型短冊、gold-500 縁、ink-700 地、白墨で文字 | hover: 青白い炎パルス、active: 0.5px 沈み |
| Button (secondary) | 線のみ、gold-700 縁、地は透過 | hover: gold-300 縁 |
| Button (ghost) | 文字のみ、下線 hover で gold-300 | hover: 下線フェードイン |
| Input | 下線のみ（box ではない）、placeholder は mist-500 | focus: 下線が gold-500 → 短くスライド |
| Textarea | 上下に 1px gold-700 ライン、四隅は開放 | focus: ラインが脈動 |
| Card | ink-700 面、`elev-1` | hover: `elev-2` |
| Modal | 障子型（半透明 washi）+ 格子オーバーレイ | open: `dur-slow` ease-out |
| Toast | 灯火型（細長、左に縦の朱ライン） | 上から滑り込み、`dur-base` |
| Tab | 札を並べたような横並び、選択中は前に出て金縁 | 切替: `dur-fast` |
| Avatar | radius-full、外周に細い金リング | online dot: 苔色 |
| Badge | 印章型、印泥色（朱）または金 | — |
| Form Error | 赤い小さな札（朱地・金文字） | shake `dur-fast`（reduced-motion 時は省略） |

---

## 12. アクセシビリティ最低基準

- WCAG 2.1 **AA** を最低ライン
- コントラスト：本文 4.5:1、大文字 3:1（§3.4 の表で担保）
- フォーカスリング：常時可視。`outline: 2px solid gold-500; outline-offset: 2px`
- キーボード：すべての操作要素に Tab 到達 + Enter/Space 起動
- `prefers-reduced-motion: reduce` を尊重（§8）
- 縦書き要素には `aria-label` で横書き等価を付ける（御札の「参拝」→ `aria-label="参拝（ログイン）"`）
- 色だけで情報を伝えない（エラーは色＋アイコン＋テキストの3点で示す）

---

## 13. 実装規約

### 13.1 CSS 変数（global.css）

```css
:root {
  /* color: ink */
  --color-ink-950: #0a0509;
  --color-ink-900: #14090f;
  --color-ink-800: #1f1219;
  --color-ink-700: #2a1c25;
  --color-ink-600: #3a2733;

  /* color: mist */
  --color-mist-100: #e7eef3;
  --color-mist-300: #b8cad8;
  --color-mist-500: #6f8fa6;
  --color-mist-700: #3d5566;
  --color-mist-glow: #b8e0ff;

  /* color: vermilion / crimson / gold / washi / moss */
  --color-vermilion-500: #b04438;
  --color-crimson-500: #c1283a;
  --color-gold-100: #f5e7c4;
  --color-gold-300: #e0c684;
  --color-gold-500: #b89540;
  --color-gold-700: #8a6d24;
  --color-washi-50: #f5efe1;
  --color-washi-700: #5c5340;
  --color-moss-500: #6b8a4e;

  /* semantic */
  --bg-base: var(--color-ink-950);
  --bg-surface: var(--color-ink-800);
  --bg-elevated: var(--color-ink-700);
  --text-primary: var(--color-gold-100);
  --text-secondary: var(--color-mist-300);
  --text-muted: var(--color-mist-500);
  --border-base: var(--color-ink-600);
  --border-strong: var(--color-gold-700);
  --accent: var(--color-crimson-500);

  /* radius */
  --radius-none: 0;
  --radius-sm: 2px;
  --radius-md: 4px;

  /* duration */
  --dur-fast: 200ms;
  --dur-base: 400ms;
  --dur-slow: 800ms;
  --dur-atmo: 1500ms;

  /* easing */
  --ease-out: cubic-bezier(0.22, 1, 0.36, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);

  /* elevation */
  --elev-1: inset 0 0 0 1px rgba(184, 149, 64, 0.18);
  --elev-2: 0 0 24px 0 rgba(184, 224, 255, 0.10),
            inset 0 0 0 1px rgba(184, 149, 64, 0.30);
  --elev-3: 0 0 48px 0 rgba(184, 224, 255, 0.18),
            0 8px 32px 0 rgba(10, 5, 9, 0.7);
}

@media (prefers-reduced-motion: reduce) {
  :root {
    --dur-fast: 1ms;
    --dur-base: 1ms;
    --dur-slow: 1ms;
    --dur-atmo: 1ms;
  }
}
```

### 13.2 Tailwind 設定（抜粋）

```ts
// tailwind.config.ts
export default {
  theme: {
    extend: {
      colors: {
        ink: { 950:'#0a0509', 900:'#14090f', 800:'#1f1219', 700:'#2a1c25', 600:'#3a2733' },
        mist: { 100:'#e7eef3', 300:'#b8cad8', 500:'#6f8fa6', 700:'#3d5566', glow:'#b8e0ff' },
        vermilion: { 300:'#d99c8c', 500:'#b04438', 700:'#621c14' },
        crimson: { 300:'#de7f8a', 500:'#c1283a', 700:'#8a0d1c' },
        gold: { 100:'#f5e7c4', 300:'#e0c684', 500:'#b89540', 700:'#8a6d24' },
        washi: { 50:'#f5efe1', 100:'#ebe2c9', 200:'#d8c9a3', 700:'#5c5340', 900:'#2e2818' },
        moss: { 500:'#6b8a4e' },
      },
      fontFamily: {
        display: ['"Yuji Syuku"', 'serif'],
        serif: ['"Shippori Mincho B1"', 'serif'],
        sans: ['"Noto Sans JP"', 'sans-serif'],
        mono: ['"JetBrains Mono"', 'monospace'],
      },
      fontSize: {
        'display-xl': ['64px', { lineHeight: '1.1' }],
        display: ['48px', { lineHeight: '1.15' }],
      },
      transitionTimingFunction: {
        out: 'cubic-bezier(0.22, 1, 0.36, 1)',
        spring: 'cubic-bezier(0.34, 1.56, 0.64, 1)',
      },
      boxShadow: {
        elev1: 'inset 0 0 0 1px rgba(184, 149, 64, 0.18)',
        elev2: '0 0 24px 0 rgba(184,224,255,0.10), inset 0 0 0 1px rgba(184,149,64,0.30)',
        elev3: '0 0 48px 0 rgba(184,224,255,0.18), 0 8px 32px 0 rgba(10,5,9,0.7)',
      },
    },
  },
};
```

### 13.3 ファイル配置

```
apps/web/src/styles/
├ global.css            CSS 変数 + リセット + 共通
├ tokens.css            （上記 :root を分離してもよい）
└ fonts.css             @font-face 定義
apps/web/src/components/ui/
├ Button.astro
├ Input.astro
├ Card.astro
├ Modal.astro
└ Toast.astro
```

---

## 14. 確定の必要な小項目（次に決める）

このシステムを承認するなら、続けて以下を詰めると各画面に着手できます：

1. ロール別の視覚記号（admin の紋・editor の印・member の札の色など）
2. ログイン画面のレイヤーごとの動作タイミング（フレーム単位）
3. ハブ画面の襖開閉の方向・速度・「奥」へのカメラ移動量
4. Wiki の和紙トーン採用範囲（本文のみ / コンテンツ全領域 / 全面ダーク統一に切り戻す）
5. エラー/404/403 ページのコピー & ビジュアル
6. ローディング演出のパターン（インライン / フルスクリーン）

---

## 15. 一行サマリー

**闇を地に、光を点に、空白を声にする。色・字・形・動・余白の五本柱で、機能性を犠牲にせず妖の世界に引き込む。**
