# 引き継ぎ資料 v2 レビュー & 改善提案

レビュー観点：**Bot連携は後回し、まずWebサイトをMVP段階まで作りきる。後からBotを足しても破壊的変更にならない設計か** を最重要に、設計の穴・抜け漏れ・運用リスクを洗います。

---

## 0. 総評（先に結論）

全体としてよく整理されており、Bot有無の分離方針はそのまま走らせて問題ありません。ただし**「Botを後から足すための具体的なコード/スキーマ上の境界線」が言語化されていない**のが最大の弱点です。今のままだと、Bot追加時に「やっぱりこのテーブル足りない」「インターフェースが直接OAuth呼んでて差し替えられない」が起きやすい。

加えて**ログイン画面の素材調達計画と、画像/フォントの実装パイプライン**、**Discord OAuth scopeとロール取得方法の具体**、**観測性（ログ・エラー追跡）**、**コスト試算**が未記載です。MVP着手前に詰めておくと後で楽です。

以下、優先度順に。

---

## 1. クリティカル：MVP着手前に解決すべき5点

### 1.1 Discord ロール取得の具体方法を確定させる（資料の前提を補強）

資料は「Bot無しでも `ログイン時取得 + TTLキャッシュ` で動く」前提ですが、**実際に何のAPIで何のscopeで取るか**が書かれていません。Bot後付けを綺麗にやる上でもここを決めておきたい。

**結論からの推奨**：

- OAuth2 scope に `identify email guilds.members.read` を**最初から付ける**
- Auth.js Discord provider のデフォルトは `identify email` だけなので明示的に追加が必要
- ロール取得は `GET /users/@me/guilds/{GUILD_ID}/member` を Discord access_token で叩く（**Botが不在でも本人ならrolesと joined_at が取れる**）
- これにより M2（マイページ：ロール・入鯖日表示）が **Bot完全不要** で成立する

この方針なら、後からBotを足したときの差分は「リアルタイムロール更新が来るので TTL を実質0にできる」だけになり、互換性破壊はゼロになります。

**追記すべき項目**：
- 必要な scope 一覧と用途
- ロール取得のエンドポイント
- access_token の暗号化保管方針（DBに raw のまま入れない）

### 1.2 Bot後付けのための「インターフェース境界」を明文化する

資料には「DB経由で疎結合」と書かれていますが、**アプリコード側の境界線**が未定義です。後でBotを足したときに既存コードを書き換える必要がない構造を、最初から決めておきます。

**推奨：リポジトリ層を1枚噛ませる**

```
src/lib/repos/
  ├ membership.ts   // getMembership(userId): { roles[], joinedAt, nickname }
  ├ activity.ts     // getActivity(userId, range): null | DailyActivity[]
  └ notifications.ts // enqueueNotification(payload)
```

各リポジトリは **データ源を切り替え可能** にする：

| リポジトリ | Bot無しMVP の実装 | Bot有り時の実装 |
|-----------|------------------|----------------|
| `membership` | OAuth API + TTLキャッシュ | DB（Botがpush） |
| `activity` | `null` を返す（UI側で「準備中」表示） | DB |
| `notifications` | DBにinsertするだけ（誰も読まない＝no-op） | DBにinsert（Botがpoll） |

UIは常にリポジトリ越しにしか触らない。**Bot追加の差分は実装ファイルだけで、呼び出し側コードはゼロ変更**になります。

### 1.3 DBスキーマは「Bot有り想定」で最初から全テーブルを作る

「後で足す」と migration のレビューが面倒になり、CFのpreview branch運用ともずれます。**Bot未使用のテーブルもMVPから作っておく**のを推奨：

- `users` / `sessions`（Auth.js）
- `members`（user_id, joined_at, nickname, last_role_sync_at）
- `roles`（discord_role_id, name, color, sort_order）
- `member_roles`（member_id, role_id）
- `wiki_pages` / `wiki_revisions`
- `wiki_visibility`（page_id, required_role_id）
- `activity_daily`（user_id, date, message_count, reaction_count）← MVPでは空のまま
- `notifications`（id, payload, created_at, sent_at, error）← MVPでは空のまま
- `audit_log`（user_id, action, target, at）

`activity_daily` と `notifications` は空のままMVPで放置。後でBotが書き始めれば即動きます。

### 1.4 ロール権限モデル：DiscordロールIDとアプリロールのマッピングを別レイヤーに

「幹部限定」「役職・階級ロールの視覚的区別」が未決定リスト入りしていますが、**コード内に discord_role_id をハードコードしないルール**を最初に敷くべきです。

```
roles テーブル + role_aliases テーブル
  role_aliases: alias='admin' → role_id=...
  role_aliases: alias='editor' → role_id=...
```

アプリは `hasRole(user, 'admin')` のように **alias で判定**。Discord 側でロールが作り直されてもDB更新で吸収できます。
ハブの「幹部だけ襖3枚」分岐もこの alias を見るだけ。

### 1.5 認証フローの未確定箇所を埋める

資料に「詳細な認証フローは別途設計時に詰める」とありますが、最低限これだけはMVP前に決めておかないと実装で詰まります：

- access_token の保管：暗号化して DB（Auth.js の Drizzle adapter で `accounts` テーブルに入る形が標準）
- refresh_token の利用タイミング：API呼び出し直前に有効期限チェック → 切れていたらリフレッシュ
- リフレッシュ失敗時のUX：強制ログアウト → ログイン画面に「お祓いが切れました」演出
- ログアウト時の挙動：sessions を即削除、Discord token はそのまま（revoke までやるかは任意）
- セッションcookie：`HttpOnly; Secure; SameSite=Lax`、`__Host-` プレフィックス検討

---

## 2. 設計の抜け漏れ（中優先）

### 2.1 ログイン画面の素材調達計画がない

「彼岸花・鳥居・霧」のレイヤー構成は決まっていますが、**素材をどう用意するか**が完全に未記載です。Three.js v2 はともかく、GSAP MVPでも素材は要ります。

選択肢：
- AI画像生成（Midjourney等）→ レイヤー分離は手作業 or Photoshopジェネレーティブ塗りつぶし
- フォトバッシュ（フリー素材合成）
- イラストレーター発注
- SVG手描き（軽量だが質感は出にくい）

**推奨**：MVPはAI生成 + 手作業レイヤー分離。各レイヤーをWebP/AVIFで書き出し、解像度2系統（mobile/desktop）。素材ライセンスと再生成のしやすさをドキュメント化。

### 2.2 prefers-reduced-motion 時の具体仕様が薄い

「簡易版に切り替え」とあるだけで何を切るか不明。具体化案：

- `reduce` 時：パララックス停止、御札の揺れ停止、フェードのみ残す
- 低スペック検出：`navigator.hardwareConcurrency < 4` または `navigator.deviceMemory < 4` で簡易モード
- ユーザー設定で「演出をオフ」を localStorage に保存できる UI（マイページ設定欄に）

### 2.3 Wiki編集のセキュリティ

「Markdown直書き / Tiptap」が未決定ですが、**どちらにせよ XSS 対策の方針が抜けている**：

- Markdown：レンダリング後に `rehype-sanitize` で許可タグ制限
- Tiptap（HTML出力）：保存時 + レンダリング時に DOMPurify
- 画像URLのallowlist（任意ドメイン埋め込みは禁止）
- `<iframe>` を Notion 埋め込みのために許可するなら、`allowlist=['notion.so']` で限定

### 2.4 楽観ロックは入れる（30人でも）

「最後勝ちでOK」は同意ですが、**revision_id ベースの楽観チェック**は入れたほうがいい（実装コスト微小、事故防止効果大）：

```
保存時に WHERE revision_id = :clientRevId を付け、
更新行数=0 なら「他の人が編集しました、再読み込みしてください」
```

### 2.5 ハブのカーソル追従の負荷ガード

「常にカーソルに呼応」は良いコンセプトですが、**スマホでは別UI**が必要。タッチには「カーソル」がない。

- スマホ：襖をタップで個別に開く / スワイプで切替
- PC：マウス追従
- 検出は `(hover: hover) and (pointer: fine)` メディアクエリ

### 2.6 エラー・ローディング・404 の世界観演出（資料の保留事項）

未決定リスト入りしていますが、**MVP段階で必須**です（特に OAuth 失敗時に標準ブラウザエラーが出ると萎える）。
推奨：MVPでは「霧の向こうから何も返ってこなかった…」程度の専用ページを1〜2枚だけ用意。深い演出はv2で。

### 2.7 SEO/インデックス

メンバー限定サーバーなので：
- ログイン画面以外は `<meta name="robots" content="noindex,nofollow">`
- `/wiki/*` `/me` `/admin/*` は `robots.txt` で Disallow
- ログイン画面は noindex でも良い（外部に世界観を見せたくないなら）

---

## 3. 運用・観測性・コスト（資料に追加すべき項目）

### 3.1 観測性（完全に抜けている）

最低限：
- **エラー追跡**：Sentry の Cloudflare Workers integration（無料枠あり）
- **アクセスログ**：Cloudflare の Workers Logs / Logpush
- **Web Vitals 計測**：CF Web Analytics または PostHog（無料枠）
- **健康監視**：UptimeRobot 等で `/api/health` を5分間隔ping

### 3.2 コスト試算（未記載）

2026年時点のざっくり月額（USD想定）：

| 項目 | 想定 | 月額 |
|-----|------|------|
| Cloudflare Pages/Workers | Free または $5 Workers Paid | $0〜5 |
| Neon Postgres | Free（0.5GB）または Launch $19 | $0〜19 |
| ドメイン | `.com` 等 | ~$1 |
| Sentry / Logpush | Free 枠 | $0 |
| VPS（Bot採用時） | さくらVPS 1GB / Vultr 等 | $5〜10 |
| **小計（Bot無し）** | | **$1〜25** |
| **小計（Bot有り）** | | **$6〜35** |

500人規模ならFree枠でも収まる可能性が高いですが、Auth.js のセッション/account行が増えてNeon Free（0.5GB）を超えないかは要監視。

### 3.3 環境分離

`AUTH_SECRET` などのシークレットを **dev/preview/production で別々に**。
Neon は branch 機能でDB自体も分離可能。Drizzle migration は preview branch に対して実行 → 動作確認 → main へ。

### 3.4 個人情報・GDPR的観点（小規模でも）

- DBに保存するもの：Discord user_id, email, ロール, 自己紹介、最終ログイン
- 退会時の削除フロー（マイページに「データ削除」ボタン）を MVP に入れておく
- Discord 側で BAN されたユーザーの扱い：ログイン拒否 + DB行は残す（audit目的）か論理削除

---

## 4. ドキュメントとして抜けている節（追記推奨）

| 節 | 内容 |
|----|------|
| **データモデル** | 上記の全テーブルとリレーション（DBML or Mermaid ERで） |
| **ディレクトリ構成** | `src/lib/repos/`、`src/components/`、`src/pages/` の方針 |
| **CSS方針** | Tailwind / vanilla / scoped Astro CSS どれか |
| **テスト方針** | MVP段階でやるか・やらないか（やらないなら明記） |
| **アクセシビリティ最低基準** | WCAG 2.1 AA を目標とするか |
| **i18n** | 日本語固定でいいか（明記しておくと後で揉めない） |
| **ブラウザサポート範囲** | モダンEvergreenのみで割り切るか |

---

## 5. 未決定事項の優先度づけ（資料の§11への補強）

| 項目 | MVP前に決める | M6以降でOK |
|-----|--------------|-----------|
| Bot採用の最終判断 | ✗ | ✓ |
| 黄泉平坂と参拝モチーフの神道整合 | △（コンセプト調整なら早めに） | |
| 役職ロールの視覚区別 | ✓（alias決め） | |
| 運営ダッシュボードの中身 | ✗ | ✓ |
| Wiki編集UI（Markdown vs Tiptap） | ✓ | |
| 活動量データ保持期間 | ✗ | ✓ |
| VPSスペック | ✗ | ✓ |
| エラー/404の世界観演出 | ✓（簡易版だけでも） | |

---

## 6. 推奨される作業順序（M0〜M5の中の細分化）

1. **M0a**: モノレポ or 単一リポ決定 → `pnpm` workspace 推奨（Web/Bot 共有 schema用）
2. **M0b**: Drizzle schema定義（§1.3 の全テーブル）→ Neon にmigration流す
3. **M0c**: Cloudflare Pages 雛形デプロイ（"Hello"だけ）
4. **M1a**: Auth.js + Discord OAuth、scopeに `guilds.members.read` を含む
5. **M1b**: ログイン後 `/me` で自分の Discord roles と joined_at が表示できる
6. **M1c**: リポジトリ層（§1.2）を導入し、ログイン画面〜マイページがリポジトリ越しに動作
7. **M2**: マイページ完成（自己紹介編集 + ロール表示 + 入鯖日 + 設定）
8. **M3a**: Wiki スキーマ + 表示
9. **M3b**: ロール別閲覧制限
10. **M3c**: Web編集 + revisions
11. **M4**: ログイン画面のGSAP演出
12. **M5**: 運営ダッシュボードのガワ + ハブ襖UI

この順だと、**M3完了時点で「Botなしでも遊べるサイト」**が成立します。

---

## 7. 一行サマリー

> **やること**は資料の方針で正しい。**やる前に決めること**として、(a) OAuth scope と membership API の確定、(b) リポジトリ層という抽象境界の導入、(c) 全テーブルのMVP前作成、(d) ログイン画面の素材調達計画、(e) 観測性とコストの記載、の5点を本資料に追記しておけば、Bot を後から足しても破壊的変更ゼロでいけます。
