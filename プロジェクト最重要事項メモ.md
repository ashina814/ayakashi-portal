# プロジェクト最重要事項メモ（ayakashi-portal）

最後まで作り切るうえで、**外したら全体が崩れる**・**忘れたら詰まる**ポイントを集約。
v3 / デザインシステム / 詳細設計 を補強するチートシートとして使う。

---

## 設計の根本

- **Bot は後付け前提**。Web 単独で MVP が完結する設計を保つ。Bot 必須機能（活動量・通知）は UI 側で「準備中」表示にしておく
- **リポジトリ層 `apps/web/src/lib/repos/` を必ず噛ませる**。UI から DB / Discord API を直接叩いたら、後で Bot を足したときに UI ごと書き直しになる
- **DB スキーマは Bot あり想定で最初から全部作る**。`activity_daily` / `notifications` も空のまま用意。後から ALTER は事故が増える
- **`discord_role_id` をハードコードしない**。必ず `role_aliases` 経由で `hasRole(user, 'admin')` の形で判定する

## Cloudflare Workers の制約

- **Node 専用 API は使わない**：`fs` / `child_process` / `crypto`（Node 版） 等
- **crypto は Web Crypto API**。Auth.js のトークン処理で詰まりがち
- **Sharp は動かない**。画像処理はビルド時 or Cloudflare Images に逃がす
- **DB 接続は `@neondatabase/serverless`**。`pg` は CF Workers では動かない（Bot 側 VPS は `pg` で OK）
- **Astro は SSR モード + `@astrojs/cloudflare` adapter**。Node API を呼ぶ npm パッケージは要注意

## 認証・ロール（最頻の地雷）

- **OAuth scope は `identify email guilds.members.read`**。Auth.js Discord provider のデフォルトは前2つだけなので明示追加が要る
- **ロール取得**：`GET /users/@me/guilds/{GUILD_ID}/member` を access_token で叩く。**Bot 不在でも本人なら自分の roles と joined_at が取れる**（これが Bot 無し MVP の柱）
- **TTL キャッシュ**：幹部 15 分・一般 1 時間。強制再取得は「運営ダッシュボード入室時」と「お祓いボタン」
- **refresh_token 失敗時**：強制ログアウト → ログイン画面に「お祓いが切れました」演出を 1 度だけ表示
- **Cookie 属性**：`HttpOnly; Secure; SameSite=Lax; __Host- prefix`
- **クライアントから role を書ける API は作らない**。役職改ざん防止

## セキュリティ

- **`accounts.access_token` / `refresh_token` は将来的に暗号化**。カラム名はそれを見据えて命名（例：`access_token_encrypted`）。`TOKEN_ENCRYPTION_KEY` を `.env.example` に最初から入れる
- **Wiki 編集の出力は必ずサニタイズ**：Markdown は `rehype-sanitize`、HTML 出力なら DOMPurify。両方 allowlist 制
- **iframe 埋め込みは allowlist 制**（Notion 等を限定許可）
- **CSRF は Auth.js 標準対策に乗る**、フォームには csrf token
- **noindex 必須**：`/me`、`/wiki/*`、`/admin/*` を `robots.txt` で Disallow + `<meta robots noindex,nofollow>`
- **secret を git に上げない**：`.env`、`*.key`、`AUTH_SECRET` は `.gitignore` で確実に除外

## データ・migration

- **timestamp は全カラム `timestamptz`（`{ withTimezone: true }`）統一**
- **id 型の方針**：Auth.js 標準テーブルは text、独自テーブルは uuid
- **`wiki_pages.current_revision_id` の循環参照**：Drizzle で素直に書くと migration が壊れる。**2段 migration**（先に nullable で作る → revisions 作成 → 後から FK / NOT NULL）
- **本番 Neon への migration は私の許可ありで**。preview branch で検証 → main → production
- **楽観ロック**：Wiki 保存時は `WHERE current_revision_id = :clientRevId`、0 件なら衝突扱い
- **個別メッセージは保存しない**（活動量は日次集計のみ）
- **退会時のデータ削除フロー**を最初から作る（30 日の論理削除 → 物理削除を推奨）

## UI・演出

- **具象イラストは使わない**。Three.js + GLSL + 抽象テクスチャだけ
- **安っぽければ option 1 へ退避**：純プロシージャル（Active Theory 系コードアート）
- **「世界が先、UI は後」**：御札・サーバー名は脇役、風景が主役
- **Three.js は client island で lazy load**（`client:idle`）。LCP に含めない
- **DPR は `min(devicePixelRatio, 1.5)` でクランプ**
- **低スペック検出**：`hardwareConcurrency < 4 || deviceMemory < 4` で粒子・octave 半減
- **「結界を抜ける」スキップ**を常時可視（reduced-motion でも）
- **画面の主役色は ink-950（地）+ gold-500（点）+ mist-glow（光）**。crimson は強調にだけ使う

## 音

- **既定はミュート**。ブラウザ autoplay policy に逆らわない
- **ユーザー操作で起動、設定は localStorage に保持**
- **`prefers-reduced-motion` 時は音もデフォルトオフ**
- **Howler.js を使う**（cross-browser、Web Audio fallback）
- **mp3 + ogg のペア書き出し**
- **音素材のライセンスは `assets/audio/CREDITS.md` に必ず記録**
- **大きな音（>-6dB）は使わない**、急激な音量変化はフェードで吸収

## アクセシビリティ

- **WCAG 2.1 AA を最低ライン**
- **`prefers-reduced-motion: reduce` 対応必須**：パララックス・揺れ・粒子・bloom を停止 or 弱める
- **フォーカスリング常時可視**：`outline: 2px solid var(--color-gold-500); outline-offset: 2px`
- **縦書き要素には `aria-label` で横書き等価**（御札の「参拝」→ `aria-label="参拝（ログイン）"`）
- **色だけで情報を伝えない**：エラーは色 + アイコン + テキストの3点

## 運用

- **環境分離**：dev / preview / production を Neon の branch で分ける
- **migration は CI で自動実行**（Drizzle Kit、preview branch 経由）
- **観測性**：Sentry（CF Workers integration）、CF Web Analytics、UptimeRobot で `/api/health`
- **コスト目安**：Bot 無しで月 $1〜25、Bot 有りで $6〜35
- **画像/音声アセットの credit 記録**を怠らない（後から発覚すると面倒）

## ハマりやすいポイント（経験則）

- Auth.js を CF Workers で動かすときは `@auth/core` を直接利用する。フレームワーク統合パッケージ（`@auth/sveltekit` 等）は使わない
- Neon の Free tier は 0.5GB。Auth.js のセッション/account 行が増えるので使用量を監視
- pnpm workspace + Windows でシンボリックリンクが詰まる場合は `.npmrc` に `shamefully-hoist=true`（M0a で対応済み）
- ESLint は **9.x flat config**（8 は EOL）
- `astro:env` を使えるなら使う（型付き環境変数）
- Three.js の `examples/jsm/...`（OrbitControls 等）は CDN にないので、自前 import が要る
- Tailwind v4 と v3 で設定書式が違う。M0a は v3 採用、変える場合は影響を見極める
- Discord OAuth の `guilds.members.read` scope は **後から追加すると既存ユーザーは再認可が必要**。最初から付けておく
- Cloudflare Pages の preview deployment では環境変数を別途設定する必要がある（自動継承されない）

## マイルストーン早見

| ID | 内容 | Bot 依存 |
|----|------|--------|
| M0a | モノレポ雛形（完了済み） | 無 |
| M0b | Drizzle schema + migration 生成 | 無 |
| M0c | Cloudflare Pages デプロイ確認 | 無 |
| M1a | Auth.js + Discord OAuth | 無 |
| M1b | `/me` で roles と joined_at 表示 | 無 |
| M1c | リポジトリ層導入 | 無 |
| M2  | マイページ完成 | 無 |
| M3a | Wiki スキーマ + 表示 | 無 |
| M3b | ロール別閲覧制限 | 無 |
| M3c | Wiki 編集 + revisions + 楽観ロック + サニタイズ | 無 |
| M4a〜e | ログイン画面 Three.js + 音 + UI + 調整 | 無 |
| M4 判定 | 「安っぽくないか」判定、必要なら option 1 へ | 無 |
| M5  | 運営ダッシュボードのガワ + ハブ襖 + エラー演出 | 無 |
| **MVP 完了線** | | |
| M6  | Bot 雛形・ロール sync | 有 |
| M7  | 活動量計測・グラフ | 有 |
| M8  | 通知キュー | 有 |
| M9  | ログイン→ハブのシネマティック遷移精緻化 | 無 |
| M10 | 運営ダッシュボードの中身 | - |

## 一行サマリー

**Bot 無し前提・リポジトリ層・OAuth scope・抽象境界・Cloudflare 制約・既定ミュート・reduced-motion・migration 慎重。これだけ守れば泥沼を回避できる。**
